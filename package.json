{
  "name": "grpc-in-container",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.19",
    "@types/node": "^26.0.0",
    "typescript": "^5.5.2",
    "vitest": "~4.1.9",
    "wrangler": "^4.104.0"
  }
}